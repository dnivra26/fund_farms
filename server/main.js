import { Meteor } from 'meteor/meteor';
import '../imports/api/plans.js';
import '../imports/api/Investments.js';
import '../imports/api/accounts.js';

Meteor.startup(() => {
  // code to run on server at startup
});
